# ==============================================================================
# FinPulse Backend — Python Dependencies
# Python 3.11+ (specify python-3.11.9 in runtime.txt for Render)
# ==============================================================================

# --- Web Framework ---
fastapi==0.111.0
uvicorn[standard]==0.30.1          # ASGI server with uvloop + websockets

# --- Database (Async) ---
sqlalchemy[asyncio]==2.0.30        # ORM with async support
asyncpg==0.29.0                    # Async PostgreSQL driver (used by SQLAlchemy)
alembic==1.13.1                    # Database migrations

# --- Configuration & Environment ---
pydantic==2.7.1                    # Data validation (FastAPI uses this internally)
pydantic-settings==2.3.0           # Settings management via .env
python-dotenv==1.0.1               # .env file loading

# --- Scheduling ---
apscheduler==3.10.4                # Background job scheduler

# --- HTTP Client (for external APIs like yFinance) ---
httpx==0.27.0                      # Async HTTP client

# --- Data & Finance ---
yfinance>=1.5.2                   # Yahoo Finance data fetcher (used in yfinance_client.py)
pandas>=2.2.2                      # Data manipulation (use >= to allow compatible wheel install)

# --- Logging ---
python-json-logger==2.0.7         # JSON-structured log formatting

# --- Testing ---
pytest==8.2.2
pytest-asyncio==0.23.7
# httpx already listed above — also used as async TestClient for FastAPI

# --- Dev Tools (not needed in production, but kept for CI) ---
# black==24.4.2
# isort==5.13.2
# flake8==7.0.0
# mypy==1.10.0
